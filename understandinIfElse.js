let userIsPrime = false;
let isUserIsDiscovery = true;

if (userIsPrime) {
  if (isUserIsDiscovery) {
    console.log(
      "You are going to see both the genre discovery and premium things."
    );
  } else {
    console.log("show only premium things.");
  }
} else {
  console.log("buy something, broteher, what's that what's that.");
}
